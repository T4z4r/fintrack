import 'package:flutter/material.dart';

class DebtScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Debt')),
      body: Center(
        child: Text('Debt Management Screen'),
      ),
    );
  }
}