import 'package:flutter/material.dart';

class AssetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Asset')),
      body: Center(
        child: Text('Asset Management Screen'),
      ),
    );
  }
}
